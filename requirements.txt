# LibertyGSM (WinDivert engine) dependencies.

# Required: pydivert bundles the WinDivert64.dll + signed .sys driver.
pydivert>=3.1.3

# Optional: system-tray support so closing the window keeps the bypass running
# in the background (like Cloudflare WARP). The app falls back to a normal
# window if these are missing.
pystray>=0.19
pillow>=10.0
